name: PageSpeedCase
on: push
jobs:
  cypress-run-api:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cypress run
        uses: cypress-io/github-action@v6
        id: cypress 
        with:
          command: npm run teste:GETapi

      - name: Enviar mensagem g-chat
        if: always()
        env:
          WEBHOOK_URL: "https://chat.googleapis.com/v1/spaces/AAAATzKXhvA/messages?key=AIzaSyDdI0hCZtE6vySjMm-WEfRq3CPzqKqqsHI&token=hKCSdPs13r28hOwKxJsTjx1c-ODv1qReIoe2VsBuR54"
        run: |
          if [ "${{ steps.cypress.outcome }}" == "success" ]; then
            status="Todos os testes passaram com sucesso!"
            icon="STATUS_GOOD" # Ícone verde
          else
            status="Alguns testes falharam."
            icon="STATUS_CRITICAL" # Ícone vermelho
          fi

          json_payload=$(echo '{
            "cards": [
              {
                "header": {
                  "title": "Resultado da Pipeline: PageSpeedCase",
                  "subtitle": "'"${status}"'",
                  "imageUrl": "https://media.istockphoto.com/id/894875516/pt/vetorial/exclamation-point-sign-in-red-triangle-vector-icon.jpg?s=612x612&w=0&k=20&c=jgIGL9n8R5jU7NturlS9AVXWi0_fUAh1n9gtpweHO1M=",
                  "imageStyle": "AVATAR"
                },
                "sections": [
                  {
                    "widgets": [
                      {
                        "textParagraph": {
                          "text": "A execução da pipeline <b>PageSpeedCase</b> foi concluída."
                        }
                      },
                      {
                        "keyValue": {
                          "topLabel": "Status",
                          "content": "'"${status}"'",
                          "contentMultiline": "false",
                          "icon": "'"${icon}"'",
                          "button": {
                            "textButton": {
                              "text": "Ver Detalhes",
                              "onClick": {
                                "openLink": {
                                  "url": "https://github.com/Rafael-Sampaio-QA/PageSpeed"
                                }
                              }
                            }
                          }
                        }
                      }
                    ]
                  }
                ]
              }
            ]
          }')

          curl -X POST -H "Content-Type: application/json" -d "$json_payload" "$WEBHOOK_URL"
          